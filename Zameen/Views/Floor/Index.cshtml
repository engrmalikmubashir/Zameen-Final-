@model IEnumerable<Floor>

@{
    ViewData["Title"] = "All Cities";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="alert alert-primary text-center">Cities</h1>

<a asp-action="Create" asp-controller="Floor" class="btn btn-outline-success mb-3"><i class="fas fa-building"></i> Add New Floor</a>

<table class="table table-hover tblLoad">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Building.Name)</th>
            <th>@Html.DisplayNameFor(m => m.Name)</th>
            <th>Actions</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(m => item.Building.Name)</td>
                <td>@Html.DisplayFor(m => item.Name)</td>
                <partial name="_TableButtonPartial" model="item.Id" />
            </tr>
        }
    </tbody>
</table>